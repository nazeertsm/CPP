#include <iostream>
using namespace std;

int main()
{

    int n = 10;

    for (int i = 0; i < n; i++)
    {
        cout<<"Loop: "<<i<<endl;
    }

    cout<<"Loop done!.."<<endl;
    //cout<<i<<endl; //"i" is undefined, if i defind outside the loop, then no error, i value can able to access outside the loop
       
}